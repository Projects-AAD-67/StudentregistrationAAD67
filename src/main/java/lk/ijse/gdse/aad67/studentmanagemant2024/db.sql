create table student(
                        id VARCHAR(50) primary key,
                        name VARCHAR(50),
                        city VARCHAR(20),
                        email VARCHAR(50),
                        level VARCHAR(20)
)
